/*
1. key types
2. key order
3. size
4. iteration
5. performance (insertion and deletion)
*/

const newMap = new Map();

newMap.set(10, "values will be here");
console.log(newMap.get(10));

console.log(newMap.keys(), newMap.values());
